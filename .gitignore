# Node
node_modules/
*.log
npm-debug.log*
.npm/

# TypeScript / build
dist/
build/
*.tsbuildinfo

# CDK
cdk.out/
cdk.context.json
.cdk.staging/

# Env / secrets (everything by default; stage files with stage values are safe to commit)
.env
.env.*
!.env.example
!.env.development
!.env.production
*.pem
*.key

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude Code (per-user, not shared)
.claude/settings.local.json

# AWS local
.aws-sam/

# Developer test fixtures (large binaries, per-machine)
Sample_Pictures/
packages/web/public/*
# ...except the permanent hero/background images used by the SPA
!packages/web/public/bg/

# House-history book scans (large, possibly copyrighted) + the artifacts
# extracted from them (persons-seed.csv, houses/*.md, persons-report.csv).
# Fed through infra/scripts/extract-house-info.ts → import-persons.ts;
# see infra/scripts/IMPORT-PERSONS.md. Per-machine source data, not app code.
House Information/

# Backlog document-import working data (per-machine, not app source).
# These hold scanned PDFs/Word docs fed through
# infra/scripts/import-documents.ts — see infra/scripts/IMPORT-DOCUMENTS.md.
DokumenterUpload/
Inbox/
_import_2017_2019/
drafts/
# Import artifacts the script writes back into the --root tree
renamed/
report.csv

# Third-party AI coding-agent scratch dirs (per-user, never shared)
.adal/
.agents/
.augment/
.bob/
.codebuddy/
.commandcode/
.continue/
.cortex/
.crush/
.factory/
.goose/
.iflow/
.junie/
.kilocode/
.kiro/
.kode/
.mcpjam/
.mux/
.neovate/
.openhands/
.pi/
.pochi/
.qoder/
.qwen/
.roo/
.trae/
.vibe/
.windsurf/
.zencoder/
skills/
skills-lock.json
.claude/skills/
.claude/worktrees/
