# Node
node_modules/
*.log
npm-debug.log*
.npm/

# TypeScript / build
dist/
build/
*.tsbuildinfo

# CDK
cdk.out/
cdk.context.json
.cdk.staging/

# Env / secrets (everything by default; stage files with stage values are safe to commit)
.env
.env.*
!.env.example
!.env.development
!.env.production
*.pem
*.key

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude Code (per-user, not shared)
.claude/settings.local.json

# AWS local
.aws-sam/
